# Compiled CLI
/deco
# Binaries dropped by `deco build`/`go build ./...` for example main packages
/example/example
/examples/router/router
# JetBrains IDE
.idea/
# Stray binaries from `deco build ./<mainpkg>` writing to the module root
/router
